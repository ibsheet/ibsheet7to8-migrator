# IBSheet7 → IBSheet8 변환 툴킷 — 빠른 시작 (Quick Start)

> 1장 요약본입니다. 자세한 내용은 [`사용가이드.md`](사용가이드.md)를 참고하세요.

---

## 1. 준비물 3가지 (설치 순서: Node → Python → AI)

| 준비물 | 설치(인터넷 가능 시) | 확인 |
|---|---|---|
| **Node.js** (LTS 18+) | [nodejs.org](https://nodejs.org) → LTS `.msi` | `node --version` |
| **Python 3** (3.8+) | [python.org](https://www.python.org/downloads/) → "Add to PATH" 체크 | `python --version` |
| **AI 도구** (택1) | Claude Code: `npm i -g @anthropic-ai/claude-code`<br>Gemini: `npm i -g @google/gemini-cli`<br>Copilot: VS Code 확장 | 도구별 |

> 사내 폐쇄망·관리자권한 없음: 인터넷 PC에서 설치 파일을 받아 전송하거나 IT 관리자에게 요청. 프록시는 `npm config set proxy http://프록시:포트`.

---

## 2. 툴킷 배치

**받는 방법 (둘 중 하나)**

| 방법 | 결과 폴더 |
|---|---|
| Git clone (접근 권한을 받은 경우)<br>`git clone https://github.com/ibsheet/ibsheet7to8-migrator.git` | `ibsheet7to8-migrator` |
| 압축 파일로 전달받은 경우 → 원하는 위치에 풀기 | 압축 파일 안의 폴더명 |

폴더를 원하는 위치에 두고, **그 폴더에서 AI 도구를 실행**합니다.
(진입점 파일 `CLAUDE.md`/`GEMINI.md`/`.github/copilot-instructions.md`이 자동 인식되어 `AGENTS.md` 규칙 적용)

> 폴더 안에서 **`helpers/ibsheet-migration.js` 만 고객 웹앱에 배포되는 산출물**입니다(5번 참고).
> `migrate.py`·`docs`·`engine`·`verify`·`samples`는 변환 작업용이라 배포하지 않습니다.

**설치 확인** — 필요한 것이 다 있는지 한 번에 점검합니다.

```
python migrate.py doctor
```

> (선택) 프로젝트별 접두사 규칙이 있으면 `migrate.config.example.json` → 대상 프로젝트 루트에 `migrate.config.json`으로 복사·수정.


> **IBSheet8 매뉴얼 최신본**은 <https://github.com/ibsheet/ibsheet8-manual> 에서 받을 수 있습니다.
> 툴킷에도 매뉴얼 전문이 포함돼 있지만, 갱신되면 변환 판단 근거가 달라질 수 있습니다.

---

## 3. 변환 실행 (자연어로 요청)

```
src/main/webapp/.../orderList.jsp 를 IBSheet8로 변환해줘
```

**어디에 입력?** 1번에서 설치한 AI 도구의 대화창에 입력합니다 — Claude Code: 터미널 `claude` 실행 후 프롬프트 / Gemini: `gemini` / Copilot: VS Code Copilot Chat 패널.
> AI가 없으면 입력할 곳이 없습니다. 이 경우 스크립트 단독으로 대신합니다 — 단, CLI와 AI는 역할이 다릅니다:
>
> | | CLI 단독 (AI 없음) | AI 자연어 요청 |
> |---|---|---|
> | 안전한 자동 변환 | ✅ | ✅ |
> | 판단 변환(Hidden→Visible 반전, 반환값 의미변경 등) | ❌ `needs_review`에 목록만 | ✅ `docs/` 근거로 처리 |
> | 완성도 | 80% (나머지는 사람이 손수정) | 완성까지 |
>
> → AI 없이 하려면 CLI 한 줄로 변환+검증까지 돌립니다.
>
> ```
> python migrate.py convert src/orderList.jsp --out build/
> ```
>
> 종료코드 **`2`** 가 나오면 **실패가 아니라** "판단 변환이 남았다"는 신호입니다.
> 옆에 생긴 `*.report.json` 의 `needs_review` 목록을 `docs/`(method/property/event-mapping) 보고
> 사람이 직접 마무리한 뒤 `python migrate.py verify build/orderList.jsp --asis src/orderList.jsp` 로 확인합니다.

- **한 번에 한 파일씩** 변환합니다(함수 누락 방지). 폴더를 주면 순차 처리.
- AI가 자동 진행: **자동 변환 → 판단 변환(docs 근거) → 자기 검증 → 리포트**.
- 결과는 원본 옆 `*.tobe.jsp` 로 생성(**원본 보존**).

---

## 4. 완료 판정 — 검증 3종 모두 PASS

| 검증 | 통과 기준 |
|---|---|
| `residue_scan` | IBSheet7 잔존 = 0 |
| `jscheck` | JS 구문 OK |
| `fn_parity` | 원본 함수 누락 = 0 |

직접 확인할 때:

```
python migrate.py verify build/orderList.jsp --asis src/orderList.jsp
```

| 종료코드 | 뜻 |
|---|---|
| `0` | 완료 |
| `1` | 검증 실패 (JS 구문 오류 또는 원본 함수 누락) |
| `2` | **판단 변환 필요** — 실패가 아니라 아직 남은 작업이 있다는 신호 |
| `3` | 사용법 오류 |

> ⚠ 의미가 크게 바뀐 변환(예: `GetSearchData`→ajax)은 **브라우저 동작 확인 필요**.
>
> ⚠ **검증 3종은 정적 검사입니다.** 아래 5번의 브릿지 헬퍼 배포가 빠져도 3종은 PASS로 나오지만 브라우저에서는 오류가 납니다.

---

## 5. 변환 후

### ★ 필수 — 브릿지 헬퍼 배포 (빠뜨리면 화면이 안 뜹니다)

변환 결과는 `helpers/ibsheet-migration.js` 의 함수(`getValue2`·`setAttribute2`·`getRowByIndex7` 등 `2`/`7` 접미사)를 호출합니다.
이 파일을 웹앱에 복사하고 **IBSheet8 본체 다음에** 불러오세요. 공통 include에 한 번만 넣으면 됩니다.

```html
<!-- 순서 중요: 본체 → 플러그인 → 브릿지 헬퍼 -->
<script src="/js/ibsheet/ibsheet.js"></script>
<script src="/js/ibsheet/plugins/ibsheet-common.js"></script>   <!-- IB_Preset 쓰면 -->
<script src="/js/ibsheet/plugins/ibsheet-excel.js"></script>    <!-- 엑셀/텍스트 다운·업로드 쓰면 -->
<script src="/js/ibsheet/plugins/ibsheet-dialog.js"></script>   <!-- 찾기·피벗 다이얼로그 쓰면 -->
<script src="/js/ibsheet/plugins/jszip.min.js"></script>        <!-- exportData/importData 쓰면 -->
<script src="/js/ibsheet/ibsheet-migration.js"></script>
```

> **★ IBSheet8은 기능이 본체 + 플러그인으로 나뉘어 있습니다.** 아래 것을 쓰면 해당 플러그인도 함께 불러와야 하며,
> 빠뜨리면 **검증 3종은 PASS인데 브라우저에서 `... is not a function`** 으로 죽습니다.
>
> | 플러그인 | 필요한 경우 |
> |---|---|
> | `plugins/ibsheet-common.js` | `IB_Preset.*`(Status·DelCheck·YMD 등) · `IBSheet.v7.convertTreeData` |
> | `plugins/ibsheet-excel.js` | `down2Excel` · `down2Pdf` · `down2Text` · `loadExcel` · `loadText` · `directDown2Excel` · `down2ExcelBuffer` · `loadExcelBuffer` 등 |
> | `plugins/ibsheet-dialog.js` | `showFindDialog` · `showPivotDialog` · `showSortDialog` 등 |
> | `plugins/jszip.min.js` | `exportData` · `importData` (본체 소속 — 서버 모듈 불필요. 없으면 **오류 없이 조용히** 안 됩니다) |
>
> **엑셀·텍스트 다운로드/업로드는 스크립트만으로는 안 되고 서버 모듈 설치도 필요합니다**(툴킷 범위 밖 — 서버 담당자에게 전달).
> **★ 서버 모듈 설치가 부담이면 `Cfg.AutoExcelMode: 2` 를 쓰세요.**
> `down2Excel()`·`loadExcel()` **코드는 그대로 두고** 설정만 바꾸면 내부적으로 클라이언트 방식
> (`exportData`/`importData`)으로 처리되어 **서버 모듈과 `Cfg.Export.Url` 이 필요 없습니다.**
> 대신 `plugins/jszip.min.js` 가 필요합니다. (`1`=서버 모듈(기본) · `2`=클라이언트 · `3`=자동 선택)

> `python migrate.py convert` 가 변환 결과를 보고 무엇이 필요한지 알려줍니다.

> **다이얼로그 참고** — IBSheet7 에 대응이 있는 것은 `ShowFindDialog`·`ShowPivotDialog` 두 개뿐입니다.
> 나머지 다이얼로그는 **IBSheet8 신규 기능**이라 변환 대상이 아닙니다.

> 변환 결과에 `Extend:IB_Preset.*`(Status·DelCheck 등)이 있으면 **`plugins/ibsheet-common.js`도 함께** 불러와야 합니다.
> 본체에 없는 전역이라, 빠뜨리면 검증 3종은 PASS인데 브라우저에서 `IB_Preset is not defined`로 죽습니다.
> CLI가 변환 결과를 보고 필요 여부를 알려줍니다.

### 이 툴킷 범위 밖 → 별도 수행

1. `*.tobe.jsp` 검토 후 반영
2. **서버**(조회/저장 엔드포인트·매퍼 SQL)가 IBSheet8 형식과 맞는지 확인·개발
3. **브라우저 QA**(렌더·조회·저장·이벤트)

---

## 막히면?

`사용가이드.md`의 **11. 문제 해결** 참고 · 기술지원 **1644-5615** (ARS 2번) · www.ibsheet.com
