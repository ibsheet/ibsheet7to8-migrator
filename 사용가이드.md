# IBSheet7 → IBSheet8 변환 툴킷 — 설치 및 사용 가이드

이 툴킷은 IBSheet7 기반 JSP/JavaScript 코드를 IBSheet8 API로 변환하는 **독립 폴더형 도구**입니다.
(특정 제품에 설치하는 플러그인이 아니라, 이 폴더를 받아서 그대로 쓰는 형태입니다.)
이 문서는 **툴킷을 처음 받은 개발자가 환경 구축부터 변환·검증까지 순서대로 따라 할 수 있도록** 작성되었습니다.

> **이 툴킷은 특정 AI에 종속되지 않습니다.** Claude Code·Gemini·GitHub Copilot 등 원하는 AI 도구를 골라 쓸 수 있습니다(2단계에서 택1). 규칙 원본은 `AGENTS.md` 하나이고, 각 AI가 자동으로 읽는 진입점 파일은 이를 가리키는 포인터입니다.

---

## 0. 이 도구가 하는 일 (1분 요약)

- IBSheet7 코드(`GetCellValue`, `SaveName`, `SearchMode:2`, `Hidden:true` 등)를 IBSheet8 코드로 바꿉니다.
- **안전한 변환은 자동**(`engine/`), **판단이 필요한 변환은 AI가 매뉴얼(`docs/`)을 근거로** 처리합니다.
- 초기화 블록(`Cfg`·`Cols`)은 **실행해서 최종 값을 읽어** IBSheet8 옵션 코드를 만들어 줍니다 — AI 없이도 됩니다.
- IBSheet7과 IBSheet8이 1:1로 안 맞는 부분(행/열 인덱스 체계, 반환값 의미 변경, 미지원 API)은 **표준 브릿지 헬퍼**(`helpers/ibsheet-migration.js`)가 흡수합니다.
- 변환 후 **스스로 검증**(`verify/`)합니다: IBSheet7 잔존 0건 / JS 구문 정상 / 원본 함수 누락 0건.

> ⚠️ 이 도구는 **화면(프론트) 코드 변환 + 정적 검증**까지 책임집니다. 서버(컨트롤러·SQL)와 실제 브라우저 동작 검증(QA)은 변환 후 별도로 진행해야 합니다. ([8단계](#8-변환-후-해야-할-일-중요) 참고)
>
> ⚠️ **변환 결과는 `helpers/ibsheet-migration.js` 파일 1개를 함께 필요로 합니다.** 이 파일을 웹앱에 배포하지 않으면 검증 3종은 PASS인데도 브라우저에서 오류가 납니다. ([8단계 1번](#8-변환-후-해야-할-일-중요) 필수)

### 폴더 구성

```
ibsheet7to8-migrator/
├── migrate.py                    ← 실행 CLI (convert / verify / rules / doctor)
├── AGENTS.md                     ← 규칙 원본(모든 AI가 이걸 따름)
├── CLAUDE.md                     ← Claude Code 진입점 (AGENTS.md 포인터)
├── GEMINI.md                     ← Gemini 진입점 (AGENTS.md 포인터)
├── .github/copilot-instructions.md ← GitHub Copilot 진입점 (AGENTS.md 포인터)
├── 사용가이드.md                  ← 이 문서
├── migrate.config.example.json   ← 프로젝트별 설정 템플릿
├── docs/      ← 지식(.md)        method/property/event-mapping, deprecated, conventions
│   ├── ibsheet7-manual/          ← IBSheet7 벤더 매뉴얼 전문 (모든 매핑 판단의 1차 근거)
│   └── ibsheet8-manual/          ← IBSheet8 벤더 매뉴얼 전문
├── helpers/   ← 런타임(.js)      ibsheet-migration.js  ★변환 대상 프로젝트에 배포 필요
├── samples/   ← 참조 예제        asis/ · tobe/ 두 쌍 + README (변환 판단 근거 예시)
├── engine/    ← 변환 엔진         migrate_core.py(치환) · init_convert.py+init_extract.js(초기화)
│                                 srcio.py(인코딩) · rules/migrate_rules.json(규칙 원본)
└── verify/    ← 검증(.py)        residue_scan.py · jscheck.py · fn_parity.py
```

> `helpers/`만 **고객 웹앱에 함께 배포되는 산출물**입니다.
> 나머지(`migrate.py`·`docs`·`engine`·`verify`·`samples`)는 변환 작업용이라 배포하지 않습니다.

> 폴더명은 Git clone 시 `ibsheet7to8-migrator` 가 됩니다. 압축 파일로 전달받았다면 그 안의 폴더명을 따르며,
> **이름이 달라도 동작에는 영향이 없습니다**(툴킷 내부는 상대 경로로만 참조합니다).

---

## 1. 준비물 한눈에 (체크리스트)

변환을 시작하기 전 아래 3가지가 PC에 갖춰져 있어야 합니다. 상세 설치는 [2단계](#2-사전-설치-상세)에서 다룹니다.

| # | 준비물 | 왜 필요한가 | 확인 명령 | 없으면 |
|---|---|---|---|---|
| 1 | **AI 코딩 도구** (택1: Claude Code / Gemini CLI / Copilot 등) | "판단 변환"을 수행 | 도구별 상이(2단계) | 자동 변환 결과에 판단 변환이 안 붙어 잔존이 남음 |
| 2 | **Node.js** (LTS 18+ 권장) | JS 구문 검증(`jscheck`) + AI CLI 도구 설치 기반 | `node --version` | 구문 검증 불가, 대부분의 AI CLI 설치 불가 |
| 3 | **Python 3** (3.8+ 권장) | 변환·검증 스크립트 실행 | `python --version` | 자동 변환·검증 스크립트 동작 불가 |

> 💡 대부분의 AI CLI 도구(Claude Code·Gemini CLI)는 **Node.js 위에서 설치**됩니다. 그래서 **Node.js를 먼저 설치**하면 2번과 1번을 한 번에 해결하기 쉽습니다. 설치 순서 권장: **Node.js → Python → AI 도구**.

---

## 2. 사전 설치 (상세)

> 아래는 **① 인터넷이 자유로운 PC**와 **② 사내 폐쇄망/제약 PC(인터넷 제한·관리자권한 없음·프록시)** 두 경우를 함께 안내합니다. 제약 환경이면 **IT/시스템 관리자의 도움이 필요할 수 있습니다.** 막히면 무리하지 말고 관리자에게 요청하세요.

### 2-1. Node.js (LTS)

JS 구문 검증과 AI CLI 도구의 기반이 됩니다.

**① 인터넷 가능 PC**
1. https://nodejs.org 접속 → **LTS** 버전 Windows Installer(`.msi`) 다운로드
2. 설치 마법사 진행(기본값 그대로 Next). "Add to PATH" 옵션은 켜진 상태 유지
3. **새 터미널을 열고** 확인:
   ```
   node --version      (예: v20.11.1)
   npm --version
   ```

**② 사내 폐쇄망/제약 PC**
- 인터넷 되는 다른 PC에서 `.msi` 설치 파일을 받아 사내 PC로 전송 후 설치
- 관리자권한이 없어 설치가 막히면 → **IT 관리자에게 "Node.js LTS 설치" 요청**
- 회사 프록시를 쓰면 npm에 프록시를 알려줘야 합니다:
  ```
  npm config set proxy http://프록시주소:포트
  npm config set https-proxy http://프록시주소:포트
  ```

### 2-2. Python 3

변환 엔진(`engine/`)과 검증 스크립트(`verify/`)를 실행합니다. **외부 패키지 설치가 필요 없는 순수 표준 라이브러리** 스크립트라, Python 본체만 있으면 됩니다.

**① 인터넷 가능 PC**
1. https://www.python.org/downloads/ → Windows용 최신 3.x 설치 파일 다운로드
2. 설치 첫 화면에서 **"Add python.exe to PATH" 체크(중요)** 후 Install
3. **새 터미널**에서 확인:
   ```
   python --version    (예: Python 3.12.2)
   ```
   > `python`이 안 먹으면 `py --version` 또는 `python3 --version`을 시도하세요.

**② 사내 폐쇄망/제약 PC**
- 인터넷 PC에서 설치 파일을 받아 전송 후 설치, 또는 IT 관리자에게 "Python 3 설치" 요청
- 사내에 이미 Python이 있다면 버전만 확인하고 그대로 사용해도 됩니다(3.8 이상 권장)

### 2-3. AI 코딩 도구 (택1)

이 툴킷은 어떤 AI를 쓰든 **동일한 규칙(`AGENTS.md`)과 검증(`verify/`)** 으로 동작합니다. 사내에서 이미 도입·허용된 도구가 있으면 그것을 쓰는 게 가장 좋습니다.

| AI 도구 | 자동 인식 진입점 | 설치(인터넷 가능 시) | 비고 |
|---|---|---|---|
| **Claude Code** | `CLAUDE.md` | `npm install -g @anthropic-ai/claude-code` | Node.js 필요. Anthropic 계정/로그인 필요 |
| **Gemini CLI** | `GEMINI.md` | `npm install -g @google/gemini-cli` | Node.js 필요. Google 계정 필요 |
| **GitHub Copilot** | `.github/copilot-instructions.md` | VS Code 확장에서 설치 | Copilot 구독 필요. 진입점 자동 인식 |
| 그 외 범용(Codex 등) | `AGENTS.md` 직접 지정 | 도구별 상이 | `AGENTS.md`를 규칙으로 지정 |

**공통 유의사항**
- 대부분 **계정 로그인 또는 API 키/구독**이 필요합니다(무료·유료 정책은 각 벤더 기준). 사내 결제·계정 정책을 먼저 확인하세요.
- 설치가 되면 툴킷 폴더에서 해당 도구를 실행합니다. 실행하면 진입점 파일(`CLAUDE.md`/`GEMINI.md`/…)이 자동 인식되어 규칙이 적용됩니다.

**② 사내 폐쇄망/제약 PC**
- 외부 AI 서비스 접속이 차단된 환경에서는 AI CLI가 동작하지 않을 수 있습니다. **사내에서 허용된 AI 도구/엔드포인트**를 우선 사용하고, 없으면 IT/보안팀과 사용 가능 여부를 협의하세요.
- AI 도구 없이도 [9단계](#9-스크립트-단독-실행--ci-배치-선택)처럼 **자동 변환 + 검증 게이트**만 스크립트로 돌릴 수 있습니다(단, 판단 변환은 사람이 직접 처리해야 함).

### 2-4. 설치 확인 (한 번에 점검)

터미널에서 아래를 실행해 세 줄 모두 버전이 나오면 준비 완료입니다.

```
node --version
python --version      (또는 py --version)
(선택한 AI 도구)      예: claude --version
```

---

## 3. 툴킷 배치 (설치 대신)

플러그인 설치 과정이 없습니다. 아래 둘 중 한 방법으로 사용합니다.

**툴킷 받기 (둘 중 하나)**

| 방법 | 결과 폴더 |
|---|---|
| Git clone (접근 권한을 받은 경우)<br>`git clone https://github.com/ibsheet/ibsheet7to8-migrator.git` | `ibsheet7to8-migrator` |
| 압축 파일로 전달받은 경우 → 원하는 위치에 풀기 | 압축 파일 안의 폴더명 |

받은 뒤 `python migrate.py doctor` 로 실행 환경(Python·Node·구성 요소)을 한 번 점검하세요.

**방법 A — 이 폴더를 작업 디렉터리로 (권장, 빠른 시작)**
1. 툴킷 폴더를 원하는 위치에 둡니다.
2. 변환할 소스를 이 폴더 안/근처에 두거나 경로로 지정합니다.
3. 이 폴더에서 AI 도구를 실행하면 진입점 파일(`AGENTS.md` 계열)의 규칙이 자동 적용됩니다.

**방법 B — 변환 대상 프로젝트에 규칙 이식**
1. 사용하는 AI의 진입점 파일(`CLAUDE.md` / `GEMINI.md` / `.github/copilot-instructions.md` 중 해당)과 `AGENTS.md`를 변환 대상 프로젝트 루트에 복사합니다.
2. 그 안의 `./engine` · `./verify` · `./docs` 경로가 이 툴킷 폴더를 가리키도록(또는 폴더째 함께 복사) 합니다.
3. 대상 프로젝트에서 AI 도구 실행.


### 벤더 매뉴얼 — 최신본 받기

이 툴킷에는 IBSheet7·8 벤더 매뉴얼 전문이 함께 들어 있습니다(`docs/ibsheet7-manual/`·`docs/ibsheet8-manual/`).
**IBSheet8 매뉴얼 최신본은 아래에서 직접 받을 수 있습니다.**

| | |
|---|---|
| IBSheet8 매뉴얼 (공개판) | <https://github.com/ibsheet/ibsheet8-manual> |

```
git clone --depth 1 https://github.com/ibsheet/ibsheet8-manual.git
```

- 매뉴얼이 갱신되면 **변환 판단의 근거가 달라질 수 있습니다.** 오래 걸리는 이관 작업이라면
  시작 시점에 한 번 최신본과 비교해 보세요.
- 툴킷의 매핑 문서는 매뉴얼을 근거로 작성돼 있으므로, 매뉴얼이 바뀌면 툴킷 갱신을
  요청해주시면 반영합니다.

---

## 4. 프로젝트 설정 (1회, 선택이지만 권장)

프로젝트마다 컬럼/폼 변수 명명 규칙이 달라서, 이를 알려주면 변환 정확도가 올라갑니다.
`migrate.config.example.json` 을 **변환 대상 프로젝트 루트**에 `migrate.config.json` 으로 복사한 뒤 수정합니다.

```jsonc
{
  // 컬럼 참조에서 떼어낼 접두사 (리스트/시트 변수 → IBSheet8 컬럼 Name엔 접두사 불가)
  "stripPrefixes": ["\\w+DTOList\\.", "\\w+Sheet\\."],

  // 폼 필드/팝업이라서 떼면 안 되는 접두사 (그대로 유지)
  "keepPrefixes": ["\\w+CommonDTO\\.", "\\w+DTO\\.", "\\w+Map\\."],

  // 조회/저장 서버 호출 URL 변환 규칙
  "urlConvention": { "search": "{screenId}Ajax.do", "save": "{screenId}Save.do" }
}
```

판단이 어려우면 설정 파일 없이 기본값으로 시작해도 됩니다. AI가 변환 중 애매한 접두사는 질문하거나 ⚠로 표시합니다.

---

## 5. 변환 실행 — 따라하기 예제

진입점 파일이 적용된 상태에서 **자연어로 요청**하면 됩니다. (별도 명령어 없음)

```
src/main/webapp/.../orderList.jsp 를 IBSheet8로 변환해줘
```

### 어디에 입력하나요?

이 요청은 **2-3단계에서 설치한 AI 도구의 대화창(프롬프트)**에 입력합니다. 별도 프로그램이 아니라 그 AI 도구 자체가 입력 창구입니다.

| AI 도구 | 입력하는 곳 |
|---|---|
| **Claude Code** | 터미널에서 `claude` 실행 → 뜨는 프롬프트에 입력 (또는 VS Code/JetBrains의 Claude Code 패널) |
| **Gemini CLI** | 터미널에서 `gemini` 실행 → 프롬프트에 입력 |
| **GitHub Copilot** | VS Code의 Copilot Chat 패널에 입력 |

> ⚠️ **AI 도구가 설치·로그인되어 있지 않으면 입력할 곳이 없습니다.** 자연어 변환은 AI가 있어야만 동작하므로 먼저 [2-3단계](#2-3-ai-코딩-도구-택1)를 완료하세요. AI를 쓸 수 없는 환경이라면 자연어 변환은 불가능하고, [9단계](#9-스크립트-단독-실행--ci-배치-선택)의 스크립트 단독 실행(자동 변환 + 검증)만 가능합니다(판단 변환은 사람이 직접 코드 수정).

AI가 `AGENTS.md`의 5단계 워크플로우(자동 변환 → 판단 변환 → 자기 검증)를 따라 처리합니다.

**여러 파일**: 정확도를 위해 **한 번에 한 파일씩** 변환합니다(함수 누락 방지). 폴더를 주면 목록을 만들어 순차 처리합니다.

```
src/main/webapp/jsp/order/ 아래 IBSheet 쓰는 JSP들을 차례로 IBSheet8로 변환해줘
```

### 변환 예시 (실제 출력 형태)

```
[변환] orderList.jsp
  자동 변환: 메서드 18건, 속성 24건
  판단 변환: 11건 (Hidden→Visible 3, ColValueDup→getRowsByDup 2, GetSelectRow→getFocusedRow 6)
  검증: 잔존 0건 PASS / 구문 OK / 함수 누락 0건 PASS
  ⚠ 동작 검증 권장: L412 GetSearchData→$.ajax 재구성 (조회 로직)
  출력: orderList.tobe.jsp
```

---

## 6. 변환이 진행되는 방식 (5단계)

1. **스코프 & 설정** — 대상 파일 확정, `migrate.config.json` 로드
2. **자동 변환** — `engine/migrate_core.py` 가 안전한 1:1 메서드·속성 변환 적용
3. **판단 변환** — 의미가 바뀌는 부분(반환값 변경, 접두사 strip, 미지원 API 등)을 AI가 `docs/` 매뉴얼 근거로 한 건씩 변환
4. **자기 검증** — `verify/` 3종 모두 통과시킴
5. **리포트** — 변환·검증 결과 + 수동 검토 권장 항목 정리

---

## 7. 결과 확인하는 법

| 산출물 | 위치 | 의미 |
|---|---|---|
| **변환 파일** | 원본 옆 `*.tobe.jsp` (기본) | **안전한 변환까지 적용된 중간 산출물**. 원본은 보존됩니다 |
| **리포트** | 변환 중 생성 `report.json` | 자동 변환 내역 + **남은 판단 항목의 위치·사유** |
| **검증 3종** | 콘솔 출력 | 아래 표 참고 |
| **★ 브릿지 헬퍼** | `helpers/ibsheet-migration.js` | 변환 결과가 **호출하는 런타임 파일**. 웹앱에 함께 배포해야 합니다 |

> ### ⚠️ `*.tobe` 파일을 그대로 배포하면 안 됩니다
>
> `*.tobe` 는 **안전한 것만 미리 적용된 초안**입니다. 판단이 필요한 자리는 **원본 코드가 그대로 남아 있습니다.**
> 예를 들어 169줄짜리 화면 하나에서 자동 20건이 적용됐고, 그 파일 안에 IBSheet7 호출이 35건 남았습니다.
>
> ```js
> mySheet.GetCellValue(row, "qty")      // IBSheet8 에 이 메서드가 없습니다
> mySheet.SetCellValue(row, "amt", …)   // 브라우저에서 "... is not a function"
> ```
>
> **남은 자리에 표시(마커·TODO)를 넣지 않습니다.** 원본 코드를 훼손하지 않기 위한 설계입니다.
> 어디를 고쳐야 하는지는 옆에 생기는 **`*.report.json` 의 `needs_review`** 에 규칙 ID·줄 번호·사유로 있습니다.
> 그것을 처리한 뒤에 배포하세요.

**입력 파일의 인코딩** — `UTF-8` 과 `cp949(euc-kr)` 를 자동으로 판별합니다.
출력은 **원본과 같은 인코딩·BOM 유무·줄끝(CRLF/LF)** 으로 씁니다. `charset=euc-kr` 인 화면을
UTF-8 로 바꿔 내보내지 않습니다(그러면 브라우저에서 한글이 깨집니다).

**IBSheet v3 화면은 건너뜁니다.** `InitDataProperty`·`InitHeadRow`·`InitRowInfo` 계열을 쓰는
화면은 IBSheet **v3** 로, IBSheet7 과 세대가 다른 제품입니다. `convert` 가 건너뛰면서
「IBSheet v3 방식 — v7→v8 변환 대상이 아닙니다」로 알립니다. 옮기려면 새로 작성해야 합니다.

> **왜 헬퍼가 필요한가**: IBSheet7의 행 인덱스는 헤더·필터·합계행을 포함하지만 IBSheet8은 데이터행만 셉니다. 열도 IBSheet7은 0부터·숨김컬럼 포함인데 IBSheet8은 1부터입니다. 이 보정을 호출부마다 손으로 넣으면 실수가 잦아, 브릿지 함수 하나로 캡슐화합니다. 변환 결과에 `getValue2`·`setAttribute2`·`getRowByIndex7`처럼 **`2`/`7`로 끝나는 함수**가 보이면 이 헬퍼의 함수입니다.

**검증 3종의 의미** (모두 PASS여야 변환 완료):

| 검증 | 통과 기준 | 실패 시 의미 |
|---|---|---|
| 잔존 스캔(residue_scan) | IBSheet7 PascalCase 메서드 0건 | 변환 누락이 남아있음 |
| 구문 검사(jscheck) | JS 구문 OK | 변환 부작용으로 코드가 깨짐 |
| 함수 파리티(fn_parity) | 원본 함수가 전부 변환본에 존재 | 함수가 통째로 누락됨 |

> **자동 변환만 돌린 직후에는 잔존 스캔이 통과하지 않는 것이 정상입니다** — 판단 항목이 아직
> 남아 있으니까요. CLI 는 이때 실패가 아니라 `미완` 으로 표시하고, 판단 변환을 끝낸 뒤 0이 됩니다.

**⚠ 동작 검증 권장 항목**: 의미가 크게 바뀐 변환(예: `GetSearchData`→ajax 재구성, 달력/색상/마우스 좌표 로직)은 변환은 됐지만 **브라우저에서 동작 확인이 필요**합니다.

---

## 8. 변환 후 해야 할 일 (중요)

이 도구의 범위는 **프론트 코드 변환 + 정적 정합성 검증**까지입니다. 화면이 실제로 동작하려면:

### 1. ★ 브릿지 헬퍼 배포 (필수 — 빠뜨리면 화면이 안 뜹니다)

`helpers/ibsheet-migration.js` 를 웹앱의 정적 리소스 경로에 복사하고, **IBSheet8 본체 다음에** `<script>`로 불러옵니다.

```html
<!-- 순서 중요: 본체 → 플러그인 → 브릿지 헬퍼 -->
<script src="/js/ibsheet/ibsheet.js"></script>
<script src="/js/ibsheet/plugins/ibsheet-common.js"></script>   <!-- IB_Preset 쓰면 -->
<script src="/js/ibsheet/plugins/ibsheet-excel.js"></script>    <!-- 엑셀/텍스트 다운·업로드 쓰면 -->
<script src="/js/ibsheet/plugins/ibsheet-dialog.js"></script>   <!-- 찾기·피벗 다이얼로그 쓰면 -->
<script src="/js/ibsheet/plugins/jszip.min.js"></script>        <!-- exportData/importData 쓰면 -->
<script src="/js/ibsheet/ibsheet-migration.js"></script>
```

> **★ IBSheet8은 기능이 본체 + 플러그인으로 나뉘어 있습니다.** 아래 것을 쓰면 해당 플러그인도 함께 불러와야 하며,
> 빠뜨리면 **검증 3종은 PASS인데 브라우저에서 `... is not a function`** 으로 죽습니다.
>
> | 플러그인 | 필요한 경우 |
> |---|---|
> | `plugins/ibsheet-common.js` | `IB_Preset.*`(Status·DelCheck·YMD 등) · `IBSheet.v7.convertTreeData` |
> | `plugins/ibsheet-excel.js` | `down2Excel` · `down2Pdf` · `down2Text` · `loadExcel` · `loadText` · `directDown2Excel` · `down2ExcelBuffer` · `loadExcelBuffer` 등 |
> | `plugins/ibsheet-dialog.js` | `showFindDialog` · `showPivotDialog` · `showSortDialog` 등 |
> | `plugins/jszip.min.js` | `exportData` · `importData` (본체 소속 함수 — `ibsheet-excel.js`·서버 모듈 불필요) |
>
> **엑셀·텍스트 다운로드/업로드는 스크립트만으로는 안 되고 서버 모듈 설치도 필요합니다**(툴킷 범위 밖 — 서버 담당자에게 전달).
> **★ 서버 모듈 설치가 부담이면 `Cfg.AutoExcelMode: 2` 를 쓰세요.**
> `down2Excel()`·`loadExcel()` **코드는 그대로 두고** 설정만 바꾸면 내부적으로 클라이언트 방식
> (`exportData`/`importData`)으로 처리되어 **서버 모듈과 `Cfg.Export.Url` 이 필요 없습니다.**
> 대신 `plugins/jszip.min.js` 가 필요합니다. (`1`=서버 모듈(기본) · `2`=클라이언트 · `3`=자동 선택)

> `python migrate.py convert` 가 변환 결과를 보고 무엇이 필요한지 알려줍니다.

> **★ `IB_Preset`을 쓰면 `plugins/ibsheet-common.js`도 필요합니다.**
> 변환 결과에 `Extend:IB_Preset.STATUS`·`IB_Preset.DelCheck`·`IB_Preset.YMD` 같은 참조가 있으면
> 그 전역은 **본체 `ibsheet.js`에 없습니다**(IBSheet8 배포본의 `plugins/` 안에 있습니다).
> 빠뜨리면 브릿지 헬퍼와 똑같이 **검증 3종은 PASS인데 브라우저에서 `IB_Preset is not defined`** 로 죽습니다.
> `IBSheet.v7.convertTreeData()`도 같은 플러그인 소속입니다.
> **CLI가 변환 결과를 보고 필요 여부를 알려줍니다** — `convert` 실행 시 「ibsheet-common.js 로드 필요」 안내가 뜨면 넣으세요.

> **다이얼로그 참고** — IBSheet7 에 대응이 있는 것은 `ShowFindDialog`·`ShowPivotDialog` 두 개뿐입니다.
> 엑셀 다운로드/업로드 등 나머지 다이얼로그는 **IBSheet8 신규 기능**이라 변환 대상이 아닙니다.

- 배포 경로는 프로젝트 규칙에 맞추면 됩니다(예: `common/ibsheet/plugins/`). 경로만 맞으면 됩니다.
- 브릿지 함수는 `IBSheet["Plugins"]` 프로토타입에 등록되어 **모든 시트 인스턴스에서 `sheet.getValue2(...)` 형태로** 호출됩니다. 화면마다 따로 넣을 필요는 없고, **공통 include에 한 번** 넣으면 됩니다.
- **검증 3종은 정적 검사라 이 파일이 없어도 PASS가 나옵니다.** 배포 누락은 검증으로 잡히지 않으니 반드시 확인하세요.

### 2. 변환 파일 반영

변환 파일(`*.tobe.jsp`)을 검토 후 원본 위치에 반영(배포)

### 3. 서버 측 확인·개발

조회/저장 엔드포인트, 매퍼 SQL 등 백엔드가 IBSheet8 요청 형식과 맞는지 확인·개발

### 4. 브라우저 QA

로그인 후 화면 렌더·조회·저장·이벤트 동작 육안 검증 (특히 ⚠ 표시 항목)

---

## 9. 스크립트 단독 실행 / CI 배치 (선택)

AI 없이 스크립트만 직접 돌릴 수도 있습니다(예: CI 파이프라인에서 잔존 검사 게이트).

### CLI 단독 vs AI — 역할이 다릅니다

터미널에서 CLI를 호출하는 것과 AI에 자연어로 요청하는 것은 **대체재가 아니라 역할이 다른 두 도구**입니다. CLI는 **안전한 변환만** 하고, 위험한 "판단 변환"은 일부러 하지 않고 `needs_review`로 목록만 남깁니다(의도된 설계).

| 구분 | CLI 단독 (AI 없음) | AI 자연어 요청 |
|---|---|---|
| 안전한 자동 변환 (메서드·속성 1:1) | ✅ 됨 | ✅ 됨 |
| 초기화 블록(`Cfg`·`Cols`) → IBSheet8 옵션 | ✅ `init_convert` 가 **코드를 만들어 줍니다**(붙여넣기는 사람) | ✅ 만들어서 반영까지 |
| 판단 변환 (`Hidden→Visible` 반전, 반환값 의미 변경, 접두사 strip, 미지원 API 재구성 등) | ❌ 안 함 — `report.json`의 `needs_review`에 **목록만** | ✅ `docs/` 근거로 한 건씩 처리 |
| 검증 3종 실행 | ✅ (터미널에서 수동 실행) | ✅ (워크플로우가 자동 실행) |

**실측 (고객 화면 542개 · 손댈 지점 32,071건)**

| 누가 처리하나 | 비율 | AI 필요 |
|---|--:|:--:|
| 엔진이 파일을 직접 고침 (안전한 치환) | **40.4%** | 불필요 |
| `init_convert` 가 IBSheet8 초기화 코드를 만들어 줌 | **23.1%** | 불필요 |
| 규칙별 지침이 `docs/` 에 있는 판단 항목 | 29.7% | 필요(또는 사람) |
| 지침이 없어 매뉴얼을 보고 판단해야 하는 항목 | 6.8% | 필요 + 사람 확인 |

> ➡️ **AI 없이도 손댈 지점의 약 63%(40.4 + 23.1)를 도구가 처리합니다.** 남은 약 37%는
> `report.json` 의 `needs_review` 목록을 `docs/`(method/property/event-mapping) 근거로 사람이 처리합니다.
>
> ⚠️ **사람 손이 전혀 안 가는 화면은 없습니다** — 542개 중 판단 항목이 0건인 화면은 **0개**입니다.
> 위 비율은 "손댈 지점(항목) 기준"이고, "화면이 그대로 동작하는 비율"이 아닙니다.
>
> ⚠️ 남은 37%는 **타이핑이 남은 것이 아니라 호출 지점의 의도에 따라 답이 갈리는 것**입니다.
> 예를 들어 `GetCellValue` 는 대개 `getValue2` 지만, 합계행·헤더·이미지 셀·`Orig` 원본값
> 문맥에서는 공식 `getValue` 를 유지해야 하고 표시 문자열이면 `getString` 입니다.
> 기계적으로 이름만 바꾸면 **오류 없이 조용히 동작이 달라집니다.** 그래서 엔진이 일부러 손대지 않습니다.


**권장 — CLI 한 줄 (변환 + 검증 3종을 함께)**

```bash
python migrate.py doctor                              # 실행 환경 점검
python migrate.py convert src/myfile.jsp --out build/ # 변환 + 검증 3종
python migrate.py convert src/ --out build/           # 디렉터리 일괄 (하위 트리 구조 유지)
python migrate.py verify build/myfile.jsp --asis src/myfile.jsp   # 검증만
```

**종료코드로 상태를 판정합니다.**

| 코드 | 뜻 | 다음 할 일 |
|---|---|---|
| `0` | 변환·검증 완료 | 없음 |
| `1` | 검증 실패 (JS 구문 오류 또는 원본 함수 누락) | 출력 파일 수정 후 재검증 |
| `2` | **판단 변환 필요** (실패 아님) | `*.report.json` 의 `needs_review` 를 `docs/` 근거로 처리 |
| `3` | 사용법 오류 | 명령 확인 |

`2` 는 정상 경로입니다. 첫 변환에는 항상 "자동으로 바꾸면 위험한 항목"이 남기 때문에,
CLI 는 그것을 **실패로 보고하지 않고 `미완`으로 표시**합니다.

```
변환 src/myfile.jsp
     자동 19건 · 검토 필요 45건
     residue    미완        ← 판단 변환이 남아 예상된 상태
     syntax     PASS
     fn_parity  PASS
```

> CLI 는 변환 결과가 브릿지 헬퍼를 호출하면 **배포 안내를 함께 출력**합니다.
> 검증 3종은 정적 검사라 헬퍼 배포 누락을 잡지 못하기 때문입니다(9번 참고).

**엔진·검증 스크립트를 직접 호출해도 됩니다**

```bash
python engine/migrate_core.py  myfile.jsp --out myfile.tobe.jsp --report report.json
python verify/residue_scan.py  myfile.tobe.jsp
python verify/jscheck.py       myfile.tobe.jsp
python verify/fn_parity.py     myfile.jsp myfile.tobe.jsp
```

**초기화 블록은 별도 명령이 코드를 만들어 줍니다 (AI 없이도 됩니다)**

```bash
python engine/init_convert.py myfile.jsp
```

IBSheet7 초기화(`IBS_InitSheet`·`SetConfig`·`InitColumns`)를 **샌드박스에서 실제로 실행해**
최종 값을 읽고, 그것으로 IBSheet8 `IBSheet.create(...)` 옵션을 만들어 화면에 출력합니다.
정규식으로 읽지 않기 때문에 줄바꿈·조건분기·문자열 조립이 섞여 있어도 값이 정확합니다.

- 출력된 옵션을 **사람이 대상 파일에 붙여넣습니다**(이 명령은 파일을 고치지 않습니다)
- `--sheet <변수명>` 으로 시트를 지정할 수 있고, `--json` 으로 추출 원본을 볼 수 있습니다
- 초기화를 부르는 쪽이 외부 파일에 있는 구조도 잡아냅니다(고객 화면 1,791개 중 **95.6%** 추출 성공)
- 옮길 때 확인이 필요한 항목은 함께 안내합니다

> 판단 변환은 여전히 남습니다. 완전한 변환은 AI 도구(`AGENTS.md`)와 함께 사용하고,
> 스크립트 단독은 **안전한 치환 + 초기화 코드 생성 + 검증/게이트** 용도로 쓰세요.

---

## 10. 규칙 커스터마이징 (고급)

회사·프로젝트 고유 패턴이 있으면 규칙을 보강할 수 있습니다.

- **자동 변환 규칙**: `engine/rules/migrate_rules.json`
  - 시그니처가 같은 1:1 메서드 → `safe_method_renames` 에 추가 (자동 변환됨)
  - 의미가 바뀌어 사람이 판단해야 하면 → `flag_for_review` 에 추가 (자동 변환 안 하고 리포트만)
- **변환 지침 문서**: `docs/*.md` 에 사례·규칙 보강 → AI가 변환 시 근거로 사용
- **규칙 원본은 `AGENTS.md` 하나** — 규칙 자체를 바꿀 때는 이 파일만 수정하면 모든 AI 도구에 동일하게 반영됩니다.

---

## 11. 문제 해결 (Troubleshooting)

| 증상 | 원인 / 해결 |
|---|---|
| `python`이 명령을 못 찾음 | PATH 미등록. `py --version` 시도, 또는 Python 재설치 시 **"Add to PATH" 체크**. 설치 후 **새 터미널**을 열 것 |
| `node`가 명령을 못 찾음 | Node.js 미설치/PATH 미등록. 재설치 후 새 터미널 |
| AI CLI 설치 실패(`npm install` 오류) | 프록시 미설정(2-1 참고) 또는 관리자권한 부족. 사내망이면 IT 관리자 요청 |
| **검증 3종은 PASS인데 브라우저에서 `getValue2 is not a function` (또는 `setAttribute2`/`getRowByIndex7` 등)** | **브릿지 헬퍼 미배포.** `helpers/ibsheet-migration.js`를 웹앱에 복사하고 IBSheet8 본체 **다음에** `<script>`로 불러오세요([8단계 1번](#1--브릿지-헬퍼-배포-필수--빠뜨리면-화면이-안-뜹니다)). 검증 3종은 정적 검사라 이 누락을 잡지 못합니다 |
| 브릿지 헬퍼를 넣었는데도 함수를 못 찾음 | ① 로드 순서 확인 — IBSheet8 본체(`ibsheet.js`)가 **먼저** 와야 합니다(헬퍼가 `IBSheet["Plugins"]`에 등록) ② 브라우저 개발자도구 Network에서 파일이 404가 아닌지 확인 ③ 캐시 삭제 후 재시도 |
| 메뉴(`setActionMenu`)를 호출했는데 아무 일도 안 일어남 | 시트 초기화 옵션에 `Def.Row`가 없으면 헬퍼가 조용히 무동작합니다. `OPT.Def = { Row: { Menu: {Items: []} } }` 를 선언하세요(`samples/tobe/orderList.jsp` 참고) |
| 잔존 스캔이 FAIL | 자동 변환만 돌리면 "판단 변환" 대상이 남아 FAIL이 정상. AI로 끝까지 진행하면 0이 됨 |
| 구문 검사(jscheck) FAIL | 변환 부작용으로 JS가 깨짐. 리포트의 해당 라인 재검토 후 재검증 |
| 함수 파리티 FAIL | 원본 함수가 변환본에 누락. **한 번에 한 파일씩** 규칙을 지켰는지 확인 |
| 한글이 깨짐 | 툴킷은 `UTF-8`·`cp949` 를 판별해 읽고 **원본과 같은 인코딩으로** 씁니다. 깨진다면 외부 도구로 직접 치환한 경우입니다 — PowerShell `Get-Content`/`Set-Content` 는 기본 인코딩이 달라 한글을 깨뜨릴 수 있으니 툴킷을 통해 변환하세요 |
| 「IBSheet v3 방식 — 변환 대상이 아닙니다」로 건너뜀 | 그 화면은 IBSheet **v3**(`InitDataProperty` 계열)입니다. v7→v8 변환 대상이 아니며, 옮기려면 새로 작성해야 합니다 |
| `init_convert` 가 「실행 실패」를 표시하고 줄 번호를 알려줌 | **구문 오류**로 표시되면 원본 코드의 문법이 그 줄에서 깨진 것입니다(브라우저에서도 같은 오류). 원본을 고친 뒤 다시 실행하세요. **실행 중 오류**로 표시되면 문법 문제가 아니라 그 화면이 다른 파일에 있는 것(라이브러리가 확장한 메서드·전역 프레임워크)에 기대는 경우로, 화면 자체는 정상일 수 있습니다 |
| `init_convert` 가 「초기화 호출을 찾지 못했습니다」 | 시트 생성·초기화가 **다른 파일**(공통 js·include)에 있는지 확인하고, 시트 변수 자동 탐지가 실패했다면 `--sheet <변수명>` 으로 지정하세요 |
| 사내망에서 AI 접속 차단 | 허용된 사내 AI 도구 사용, 또는 [9단계] 스크립트 단독(검증 게이트)으로 운용 |

---

## 12. 자주 묻는 질문 (FAQ)

**Q. 원본이 바뀌나요?**
A. 아니요. 기본은 원본 옆에 `*.tobe.<확장자>` 로 새로 만듭니다. 덮어쓰기는 사용자가 확인한 경우에만 합니다.

**Q. 변환하면 바로 화면이 동작하나요?**
A. 아니요. `*.tobe` 는 **안전한 변환까지만 적용된 초안**이고, 판단이 필요한 자리는 원본 코드가 그대로 남아 있습니다. `*.report.json` 의 `needs_review` 를 처리해야 동작합니다. 그 뒤에도 서버(백엔드)와 브라우저 동작 검증은 별도로 필요합니다([8단계](#8-변환-후-해야-할-일-중요)).

**Q. 어느 정도가 자동으로 변환되나요?**
A. 고객 화면 542개로 실측한 결과, 손댈 지점 32,071건 중 **약 63%를 도구가 처리**합니다 — 엔진이 파일을 직접 고치는 **40.4%** 와, `init_convert` 가 IBSheet8 초기화 코드를 만들어 주는 **23.1%** 입니다. **AI 없이 폐쇄망에서도 이만큼 됩니다.**

다만 **사람 손이 전혀 안 가는 화면은 없습니다**(542개 중 0개). 위 수치는 "항목 기준"이고 "화면이 그대로 동작하는 비율"이 아닙니다. 남은 약 37%는 타이핑이 남은 것이 아니라 **호출 지점의 의도에 따라 답이 갈리는 항목**이라, 기계적으로 바꾸면 오류 없이 조용히 동작이 달라집니다.

**Q. AI를 쓰면 무엇이 달라지나요?**
A. 도구가 처리하는 63%는 같습니다. 차이는 남은 37%에서 납니다 — 그중 **29.7%p 는 규칙별 지침이 `docs/` 에 이미 적혀 있어** AI가 그 지침을 적용하는 작업이 됩니다(사람도 할 수 있지만 화면당 평균 35건을 문서 찾아가며 처리해야 합니다). 나머지 **6.8%p 는 지침이 없어** AI도 매뉴얼과 화면 맥락을 봐야 하고 **사람 확인이 필요합니다.**

**Q. 이 도구의 값은 무엇인가요?**
A. "타이핑 절감"이 아니라 **조용히 깨질 지점을 착수 전에 목록으로 뽑아 주는 것**입니다. 실측한 542개 화면 중 **97.8%** 에, 이름만 바꾸면 오류 없이 동작이 달라지는 항목이 들어 있었습니다(예: `SetColWidth` 는 IBSheet7 이 절대값 설정인데 IBSheet8 `setColWidth` 는 **가감**입니다 — 이름만 바꾸면 300px 이 되는 게 아니라 300px 넓어집니다). 이런 자리를 위치·사유와 함께 먼저 알려 줍니다.

**Q. 꼭 Claude를 써야 하나요?**
A. 아니요. Claude Code·Gemini·Copilot 등 원하는 AI를 쓰면 됩니다. 규칙(`AGENTS.md`)과 검증(`verify/`)이 동일하게 적용되어, 어떤 AI가 변환하든 결과는 같은 기준으로 판정됩니다.

**Q. Python/Node가 회사 PC에 없습니다.**
A. 설치가 어렵다면 관리자에게 요청하세요. 두 런타임이 없으면 자동 변환·구문 검증이 동작하지 않습니다.

**Q. 변환 결과에 `getValue2`, `setAttribute2`, `getRowByIndex7` 같은 낯선 함수가 있습니다. IBSheet8 API인가요?**
A. 아니요. 툴킷이 제공하는 **표준 브릿지 헬퍼**(`helpers/ibsheet-migration.js`)의 함수입니다. IBSheet7과 IBSheet8이 1:1로 맞지 않는 부분(행/열 인덱스 체계 차이, 반환값 의미 변경, IBSheet8에 없는 기능)을 흡수합니다. 이름 끝의 **`2`/`7`이 브릿지 함수 표식**입니다. 이 파일을 웹앱에 함께 배포해야 동작합니다([8단계 1번](#1--브릿지-헬퍼-배포-필수--빠뜨리면-화면이-안-뜹니다)).

**Q. 브릿지 없이 순수 IBSheet8 API만으로 변환할 수 있나요?**
A. 대부분 가능하지만 권장하지 않습니다. 예를 들어 IBSheet7 행 인덱스 → IBSheet8 행 객체 변환은 헤더·필터·상단합계 행 수를 매번 계산해야 하고, `ComputeSum`처럼 IBSheet8에 아예 없는 기능은 직접 구현해야 합니다. 호출부마다 이 보정을 흩뿌리면 실수가 잦아, 브릿지 하나로 모으는 것이 안전합니다.

**Q. 브릿지 헬퍼를 우리 쪽에서 수정해도 되나요?**
A. 가능하지만, 수정하면 툴킷 갱신 시 충돌합니다. 프로젝트 고유 로직이 필요하면 **별도 파일**에 추가하고 헬퍼 원본은 그대로 두는 것을 권장합니다.

**Q. IBSheet 라이브러리(8) 자체도 포함되나요?**
A. 아니요. 이 툴킷은 **코드 변환 도구**입니다. IBSheet8 라이브러리/라이선스는 IBSheet 벤더를 통해 별도 도입해야 합니다.

---

## 13. 범위와 한계 (요약)

- ✅ IBSheet7 → IBSheet8 **코드 변환** + 정합성 검증(잔존/구문/함수 누락)
- ✅ 안전한 변환 자동화 + 위험한 변환은 매뉴얼 근거 판단 + 자기 검증
- ✅ 초기화 블록(`Cfg`·`Cols`)은 **실행해서 값을 읽어** IBSheet8 옵션 코드를 만들어 줍니다 (AI 없이도 동작)
- ✅ `UTF-8`·`cp949` 자동 판별 · 출력은 **원본과 같은 인코딩·BOM·줄끝**
- ✅ 특정 AI 비종속(Claude/Gemini/Copilot 등 택1)
- ✅ 1:1 매핑 안 되는 부분을 흡수하는 **표준 브릿지 헬퍼** 제공 (`helpers/ibsheet-migration.js` — JS 파일 1개)
- ⚠️ **`*.tobe` 는 초안입니다.** 판단 항목(`report.json` 의 `needs_review`)을 처리해야 동작합니다. 손이 전혀 안 가는 화면은 없습니다(실측 542개 중 0개)
- ⚠️ 변환 결과는 위 헬퍼에 **런타임 의존**합니다 → 고객 웹앱에 파일 1개 배포 필요. 검증 3종(정적 검사)은 이 배포 누락을 잡지 못합니다
- ❌ **IBSheet v3 화면**(`InitDataProperty` 계열) — 세대가 다른 제품이라 대상이 아닙니다. 도구가 건너뛰며 알려 줍니다
- ❌ 백엔드(컨트롤러/매퍼 SQL) 개발, 실제 브라우저 동작/QA
- ❌ IBSheet8 라이브러리 제공/라이선스

---

## 14. 문의 / 기술지원

변환 문의, 규칙 보강, IBSheet 라이브러리/라이선스 도입은 아래로 연락해 주세요.

| 구분 | 내용 |
|---|---|
| 배포처 | 아이비시트 기술지원 |
| 회사명 | 아이비리더스 |
| 연락처 | **1644-5615** (ARS 2번) |
| 홈페이지 | www.ibsheet.com |
